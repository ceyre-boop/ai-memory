# the store itself — ciphertext, but never in git
embeddings/
# user inbox — plaintext, user-managed
corpus/
# secrets
.env
*.key
# runtime
node_modules/
.DS_Store
# parked work (not tracked)
wip/
# scheduled standing-review output — quotes from the operator's own record
reviews/
